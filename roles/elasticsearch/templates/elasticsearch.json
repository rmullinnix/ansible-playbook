{
    "service": {
        "name": "elasticsearch",
        "tags": ["master"],
        "port": {{ elasticsearch_port }},
        "check": {
            "script": "{{ inf_app_path }}/elasticsearch/current/bin/es_status {{ elasticsearch_port }}",
            "interval": "300s"
        }
    }
}
