---
# file: roles/consul/handlers/main.yml
- name: install consul
  command: /sbin/insserv -f consul{{ init_suffix }}

- name: restart consul
  service: name=consul{{ init_suffix }} state=restarted

- name: refresh consul
  service: name=consul{{ init_suffix }} state=reloaded
